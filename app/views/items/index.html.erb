<h2>List of Items</h2>
<%= link_to 'Add Item', new_item_path %>
<table>
  <tr>
    <th>Title</th>
    <th>Text</th>
  </tr>
 
  <% @items.each do |item| %>
    <tr>
      <td><%= item.title %></td>
      <td><%= item.description %></td>
    </tr>
  <%end%>
</table>



